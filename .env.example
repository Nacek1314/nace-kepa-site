# Optional: Cloudflare Worker URL that forwards order submissions to Telegram.
# If unset, the order form falls back to mailto + file download.
# This URL is public — set it as a GitHub repository *variable* (not secret).
# Setup steps: cloudflare-worker/README.md
PUBLIC_ORDER_ENDPOINT=
